Part1:
  В index.html, создать блок который будет в 2 раза выше экрана
  В index.js:
  создать функцию waitForScroll, которая возвращает Promise, выполняющийся как только пользователь проскролит страницу
  создать функцию drawOnScroll, которая использует ф-ю waitForScroll, и по выполнению обещания красит созданный блок в
  случайный цвет (это может быть случайный из набора цветов).


Part2:
  создать функцию waitForAnswer, которая возвращает Promise, выполняющийся как только пользователь нажмет на клавишу Y
  или провалится как только пользователь нажмет на клавишу N
  https://keycode.info
  создать функцию setText, которая принимает аргумент - текст который должен быть встроен на страницу или заменен
  старый (создать/заменить блок div)
  Запустить следующий алгоритм используя созданные функции:
  Вставить текст “Вы сделали домашнее задание? Y / N”
  Начать ожидать ответ пользователя с помощью waitForAnswer
  Если обещание успешно - заменить текст на “Так держать!”
  Если неуспешно - заменить текст на “Нужно подтянуть(”

Part3*:
  создать ф-ю waitForTime, которая принимает 1 аргумент - кол-во секунд, возвращает Promise, выполняющийся
  как только пойдет указанное время.
  создать ф-ю waitForClicks, которая принимает аргумент - кол-во необходимых кликов, возвращает Promise,
  выполняющийся как только пользователь выполнит нужное кол-во кликов.
  Создать ф-ю clickChallange, которая принимает 2 аргумента (кол-во кликов и лимит времени, сек),
  должна запустить следующий алгоритм используя созданные функции:
  Вставить текст “У вас есть n секунд, чтобы сделать m кликов”
  (с помощью setText, вместо букв - значения)
  Использовать ф-и waitForTime  и waitForClicks
  Если выполнено нужно кол-во кликов раньше чем вышло время
  - заменить текст на “Good job, comrade.”
  Если время вышло - заменить текст на “100 кликов вне очереди.”

